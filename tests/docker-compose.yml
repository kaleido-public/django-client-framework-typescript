version: "3"
services:
  test:
    build:
      context: ..
      dockerfile: tests/tsclient.dockerfile
  server:
    build:
      context: .
      dockerfile: ./server.dockerfile
      args:
        DCF_BRANCH: staging
        TEST_SERVER_BRANCH: master
